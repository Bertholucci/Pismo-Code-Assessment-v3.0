spring:
  application:
    name: java
  datasource:
    url: ${DATABASE_STRING_CONNECTION:jdbc:postgresql://localhost:5432/pismo}
    username: username
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
